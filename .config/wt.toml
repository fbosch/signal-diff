[aliases]
gate-push = "git push no-mistakes {{ branch }}"
gate-diff = "wt step diff"

[[aliases.gate]]
commit = "wt step commit"

[[aliases.gate]]
push = "git push no-mistakes {{ branch }}"

[pre-merge]
validate = "pnpm lint && pnpm test && pnpm typecheck"
